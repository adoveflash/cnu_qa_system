# 발표자료 작업 HANDOFF (랩실 Final Project (2))

> 이 문서 하나만 읽으면 발표자료 작업을 이어서 할 수 있도록 정리한 인수인계 문서.
> Antigravity 등 다른 에이전트에 넘길 때: "이 파일 읽고 발표자료 이어서 해줘"로 시작.

## 0. 한 줄 요약

충남대 **학내 정보 RAG Q/A 챗봇**을 지능소프트웨어 연구실 신입생 **Final Project (2)**로 발표한다.
발표자료는 `.pptx`를 직접 만지지 말고 **생성기 파이썬 스크립트를 편집 → 재생성**한다.

## 1. 작업 대상 파일

| 파일 | 역할 | 주의 |
|------|------|------|
| `make_ppt_lab.py` | **랩 발표 생성기** (이걸 수정) | 실행: `python make_ppt_lab.py` |
| `발표자료_랩실_장윤상.pptx` | 생성 결과물 (12슬라이드) | **gitignore됨**(`*.pptx`) — 커밋 안 함, 재생성으로 관리 |
| `발표대본_랩실_장윤상.md` | 슬라이드별 대본 + 예상질문 | 슬라이드 수정 시 대본도 같이 갱신 |
| `make_ppt.py` | (별개) **과제 제출용** 발표 생성기 | 랩 발표와 혼동 금지 |

- `.pptx`는 `python-pptx`로 생성. `pptx` 미설치면 `pip install python-pptx`.
- 슬라이드 폭 13.333in(16:9). 헬퍼: `title_slide / content_slide / trouble_slide(3열 표)`.

## 2. 과제 명세 (근거 원본)

- 파일: `~/Desktop/202202497/3-1/개인/LAB/신입생_튜토리얼(2025.03.11).pdf` (62p)
- **Final Project (2)** 상세: PDF **p.52~55** (7~8 week 파트)
- 요구 = "RAG를 사용한 Q/A System 구축 + Chat Interface(Streamlit) 정성 평가, working chatbot + API"

### 발표 자료 필수 6항목 (명세에 명시 — 슬라이드 골격)

1. 데이터 선정 및 분석
2. 데이터 전처리
3. 모델 선정 기준 (왜 이 문제에 이 모델?)
4. 구현 방식 설명
5. 정성 평가
6. 향후 연구 (앞으로 어떻게 할지?)

### 제약 (반드시 준수 — 발표에서 어긋나면 안 됨)

- **오픈 웨이트 모델만.** GPT·Claude 등 closed 모델 사용 금지.
- **기존 데이터셋 금지 → 직접 수집·구축.** (KorQuAD 등 공개 QA 미사용)
- Vector DB 자유(ChromaDB 선택). LLM 파인튜닝은 자율(본 프로젝트는 안 함).
- 데모는 **Streamlit** 필수.

## 3. 반드시 지켜야 할 "정정된 사실" (틀리기 쉬운 지점)

1. **모델 = Gemma 4 12B** (`google/gemma-4-12b-it`, 오픈 웨이트), 4bit NF4, **bfloat16**.
   - float16이면 활성값 오버플로로 출력 깨짐(점·쉼표). Gemma 3은 폴백일 뿐 현재 기본은 Gemma 4.
2. **파인튜닝 없음.** base 모델 직접 사용 + RAG로 지식 주입(LoRA 제거).
3. **RAG 검색 코퍼스 = 문서 청크** (`data/corpus/chunks.jsonl` → bge-m3 임베딩 → ChromaDB `cnu_chunks`).
   - 빌드: `scripts/build_db.py`.
4. **Q&A 쌍(`data/qa/*.jsonl`)은 평가용일 뿐, 추론 파이프라인에 미사용.**
   - `run_train.py`(파인튜닝) 입력이었으나 파인튜닝을 안 하므로 시스템에 안 들어감.
   - → "데이터 어떻게 만들었나" 질문엔 "크롤링 문서 청크를 색인, Q&A는 평가셋"으로 답.
   - → 여기서 Claude로 Q&A 증강한 것은 **배포 시스템과 무관**하므로 closed-모델 규정과 충돌 아님.
5. **RAG 파이프라인(Advanced RAG)**: 밀집(bge-m3)+희소(BM25) 하이브리드 → RRF 융합
   → cross-encoder(`bge-reranker-v2-m3`) 리랭크 → **점수 컷오프**(무관 청크는 컨텍스트·출처에서 탈락)
   → 전부 컷오프 시 "확인되지 않았습니다" 정직 응답. 코드: `src/rag/retriever.py`. 결정: `docs/decisions/004-*.md`.
6. **발표 핵심 스토리 = 실시간 라우팅 재설계 회고**: 키워드 매칭 → **LLM tool-calling**.
   - 키워드의 4가지 한계(정적정보 매번 조회 / '1학' 등 변형 미인식 / 의미 무시 / fallback 오답)
   - 근본 원인: 의미 판단을 LLM 아닌 사람(키워드)이 대신함.
   - 해결: Gemma가 도구+인자 직접 선택(`src/skills/router.py`), tri-state(도구선택/불필요/파싱실패→키워드 폴백).
   - 원문 회고: `DIARY.md`의 "회고 — 키워드 기반 tool-calling의 한계 (발표용)" 섹션.
7. UI = **Streamlit** (`src/app/streamlit_app.py`), 진입점 `chatbot.sh`. (Gradio는 레거시)

## 4. 현재 슬라이드 구성 (12장) — `make_ppt_lab.py`

1. 표지
2. 목표 & 과제 요구사항(제약 포함)
3. ① 데이터 선정 및 분석
4. ② 데이터 전처리 → 벡터 색인 (※ Q&A는 평가용·추론 미사용 명시)
5. ③ 모델 선정 기준 (왜 Gemma·bge-m3·ChromaDB)
6. ④ 구현 — 시스템 아키텍처
7. ④ 구현 — RAG 검색 설계(하이브리드+리랭크+컷오프)
8. ④ 구현 — 실시간 라우팅 재설계 회고(표: 키워드 4문제)
9. ④ 구현 — 키워드 → LLM tool-calling 전환
10. ⑤ 정성 평가 & 데모
11. ⑥ 향후 연구
12. 감사합니다 / Q&A

- 대본: `발표대본_랩실_장윤상.md` (슬라이드별, 총 ≈9분40초 + 데모, 예상질문 5개 포함).

## 5. 열린 작업 / 다음 후보

- [ ] 데모 슬라이드(10)에 실제 Streamlit UI 스크린샷 삽입 (현재 "삽입 위치" 자리표시자).
- [ ] 대본을 pptx 발표자 노트(slide notes)에 삽입할지 (요청 시).
- [ ] 12→~10슬라이드 압축 여부 (요청 시).
- [ ] 아키텍처(6)를 텍스트 대신 도식(도형 화살표)으로 시각화할지.

## 6. 하지 말 것

- `.pptx`를 손으로 편집하고 그걸 정본으로 삼기 → **반드시 생성기(`.py`) 수정 후 재생성**.
- Gemma 4를 Gemma 3으로, 오픈 모델을 closed로 잘못 기술.
- Q&A 데이터를 RAG 코퍼스/추론에 쓴다고 서술(사실은 평가용).
- 랩 발표(`make_ppt_lab.py`)와 과제 제출 발표(`make_ppt.py`)를 뒤섞기.
