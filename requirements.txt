# 충남대학교 Campus ChatBot — 실행 의존성
# 환경: Python 3.10.12
# 모델: Gemma 4 (google/gemma-4-12b-it, 4bit NF4)

# ── Core ML (추론 필수) ──
# Gemma 4는 torch 2.7+ 필요(modeling 코드가 torch.float8_e8m0fnu 참조).
# ★ SSH 박스(GPU 드라이버 CUDA 12.4)는 cu126 빌드로 설치해야 GPU가 잡힌다
#   (cu130 기본 빌드는 드라이버가 못 받음). 설치 예:
#   pip install torch==2.12.1 torchvision torchaudio --index-url https://download.pytorch.org/whl/cu126
# 드라이버가 더 최신(CUDA 12.8+/13)이면 기본 PyPI 빌드도 가능.
torch==2.12.1
transformers==5.12.1
accelerate>=1.0.0
bitsandbytes>=0.45.0

# 임베딩 / Vector DB
# transformers 5.x와 호환되는 sentence-transformers 3.x 이상 사용.
sentence-transformers>=3.0.0
chromadb>=0.5.0

# Advanced RAG — BM25 희소검색 (hybrid 모드). reranker는 sentence-transformers CrossEncoder 사용
rank-bm25>=0.2.2

# 모델/벡터DB 다운로드
huggingface_hub>=0.30.0

# 실시간 도구(식단·셔틀·공지 크롤링)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Web UI — Streamlit으로 전환 (gradio는 레거시 폴백으로 유지)
streamlit>=1.30.0
gradio>=4.20.0
