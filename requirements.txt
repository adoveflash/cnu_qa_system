# 충남대학교 Campus ChatBot — 실행 의존성
# 환경: Python 3.10.12 / CUDA 12.x (Colab T4 기준)
# 과제 명세 지정 버전: torch 2.5.1

# Core ML (추론 필수) — Gemma 3는 torch 2.5.1 + transformers 4.50에서 구동
torch==2.5.1
torchvision==0.20.1
torchaudio==2.5.1
transformers==4.50.0
accelerate>=1.0.0
bitsandbytes>=0.45.0

# 임베딩 / Vector DB
# sentence-transformers는 2.x로 고정 — 5.x는 torchcodec를 강제 import하는데
# torch 2.5.1과 비호환(libtorchcodec 로드 실패)이라 RAG가 죽음.
sentence-transformers==2.7.0
chromadb>=0.5.0

# 모델/벡터DB 다운로드
huggingface_hub>=0.25.0

# 실시간 도구(식단·셔틀·공지 크롤링)
requests>=2.31.0
beautifulsoup4>=4.12.0

# Web UI — Streamlit으로 전환 (gradio는 레거시 폴백으로 유지)
streamlit>=1.30.0
gradio>=4.20.0
