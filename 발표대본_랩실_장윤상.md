# 랩실 Final Project (2) 발표 대본 — 충남대 학내 정보 RAG Q/A 챗봇

- 대상: 지능소프트웨어 연구실 신입생 Final Project (2) 발표
- 발표자: 장윤상 (202202497)
- 목표 시간: 약 10분 (12슬라이드) · 데모 별도
- 톤: 존댓말, 천천히. 굵은 부분은 강조해서 읽기.

---

## 슬라이드 1 — 표지 (약 0:20)

안녕하세요, 컴퓨터융합학부 장윤상입니다.
이번 신입생 Final Project (2)로 진행한 **충남대학교 학내 정보 RAG 기반 Q/A 챗봇**을 발표하겠습니다.
학생들이 졸업요건이나 식단, 셔틀 같은 학내 정보를 자연어로 물어보면 답해 주는 챗봇입니다.

---

## 슬라이드 2 — 목표 & 과제 요구사항 (약 0:50)

먼저 과제 요구사항부터 정리하겠습니다.
이번 프로젝트의 목표는 **RAG를 사용한 Q/A 시스템을 만들고, 챗봇 인터페이스로 정성 평가**하는 것입니다.
도메인은 충남대 재학생을 위한 학내 정보로 잡았고, 졸업요건·공지·학사일정·식단·셔틀 다섯 개 영역을 다룹니다.

과제에는 세 가지 제약이 있었습니다.
첫째, **오픈 웨이트 모델만** 써야 합니다. GPT나 Claude 같은 클로즈드 모델은 금지입니다.
둘째, **기존 데이터셋을 쓰면 안 되고 직접 수집·구축**해야 합니다.
셋째, Vector DB는 자유고, 데모는 Streamlit으로 만들어야 합니다.
이 제약들을 지키면서 설계했습니다.

---

## 슬라이드 3 — ① 데이터 선정 및 분석 (약 0:55)

먼저 데이터입니다.
학내 정보는 공개된 QA 데이터셋에 존재하지 않습니다. 그리고 과제 규정상 기존 데이터셋도 못 씁니다.
그래서 KorQuAD 같은 공개 데이터는 일절 쓰지 않고, **충남대 공개 웹페이지를 직접 크롤링**해서 구축했습니다.

수집 대상은 통합 허브, 컴퓨터융합학부, 인재개발원, 생협, 식단, 기숙사 페이지입니다.
크롤링할 때는 **robots.txt를 지키고 요청 사이에 3초 이상 간격**을 뒀습니다.
정적 페이지는 requests와 BeautifulSoup으로, 자바스크립트 렌더링 페이지는 playwright로, PDF는 pdfplumber로 처리했습니다.
다섯 개 도메인을 균형 있게 모아서 특정 카테고리로 쏠리지 않게 했습니다.

---

## 슬라이드 4 — ② 데이터 전처리 → 벡터 색인 (약 0:55)

수집한 원본을 그대로 쓰진 않고 전처리했습니다.
HTML과 PDF에서 본문만 추출하고, 메뉴바나 중복, 잡음을 걷어냈습니다.
그리고 개인정보 보호를 위해 **학번·이름·연락처는 크롤링 직후에 마스킹하고 원본은 폐기**했습니다.

청킹은 300에서 500토큰 단위로, 50토큰씩 겹치게 나눴고, 문단과 제목 경계를 우선했습니다.
이 정제된 청크를 **bge-m3 임베딩으로 벡터화해서 ChromaDB에 적재**했습니다. 이게 RAG의 검색 코퍼스입니다.

여기서 한 가지 짚고 싶은 건, 저희는 **파인튜닝 없이 베이스 모델을 그대로 쓰기 때문에, 검색 대상은 Q&A 쌍이 아니라 문서 청크**라는 점입니다.
Q&A 쌍은 따로 만들긴 했지만 어디까지나 평가용이고, 실제 추론에는 쓰지 않습니다.

---

## 슬라이드 5 — ③ 모델 선정 기준 (약 0:55)

왜 이 모델들을 골랐는지 말씀드리겠습니다.
생성 모델은 **구글의 Gemma 4 12B, 오픈 웨이트 모델**을 썼습니다.
클로즈드 금지 규정을 지키면서도 한국어와 지시 이행 성능이 좋았기 때문입니다.
**4bit NF4 양자화**를 적용해서 12B 모델을 단일 GPU, 그리고 Colab T4에서도 돌아가게 만들었습니다.
그리고 파인튜닝 대신 **RAG로 최신·정확한 지식을 주입**해서 환각을 억제하는 방향을 택했습니다.

임베딩은 다국어에 강한 **bge-m3**를 썼고, VRAM을 아끼려고 임베딩은 CPU에 올려서 GPU는 12B 모델에 양보했습니다.
Vector DB는 로컬에서 돌고 제출물에 그대로 번들할 수 있는 **ChromaDB**를 선택했습니다.

---

## 슬라이드 6 — ④ 구현: 시스템 아키텍처 (약 0:45)

이제 구현입니다. 전체 구조는 과제에서 제시한 표준 RAG 파이프라인을 따릅니다.
문서를 임베딩해서 벡터 DB에 넣고, 질문이 들어오면 검색해서 LLM이 답을 만들고, 사용자는 Streamlit 웹앱으로 대화합니다.

추론 흐름을 보면,
먼저 **라우터가 실시간 정보가 필요한지 판단**합니다. 식단·셔틀·학사·공지 같은 건 실시간 조회로 갑니다.
그 다음 **RAG 검색으로 관련 청크를 모아 컨텍스트를 구성**하고,
마지막에 **시스템 프롬프트와 컨텍스트, 질문을 합쳐 Gemma에 넣어 답변과 출처**를 만듭니다.

---

## 슬라이드 7 — ④ 구현: RAG 검색 설계 (약 0:55)

RAG에서 가장 공을 들인 부분이 검색 정밀도입니다.
처음엔 단순히 top-k로 가까운 청크를 가져왔는데, **관련 없는 청크가 그대로 컨텍스트와 출처에 섞여 들어가는** 문제가 있었습니다. 엉뚱한 학과나 부서 페이지가 근거로 붙는 거죠.

그래서 Advanced RAG로 바꿨습니다.
**밀집 검색인 bge-m3와 희소 검색인 BM25를 하이브리드로 돌리고 RRF로 융합**합니다.
그 다음 **cross-encoder 리랭커로 다시 점수를 매겨서**, static과 live 컬렉션을 같은 스케일로 비교합니다.
마지막으로 **점수 컷오프를 둬서, 관련도가 낮은 청크는 컨텍스트뿐 아니라 출처 배지에서도 탈락**시킵니다.
만약 전부 컷오프되면 억지로 답을 지어내지 않고 "확인되지 않았습니다"라고 정직하게 답합니다.

---

## 슬라이드 8 — ④ 구현: 실시간 라우팅의 재설계 (회고) (약 1:05)

이 슬라이드가 제가 이번 프로젝트에서 가장 많이 고민한 부분입니다.
처음엔 실시간 스킬을 **키워드 매칭**으로 라우팅했습니다. 질문에 특정 키워드가 있으면 학교 서버를 조회하는 방식이죠.
그런데 실제로 돌려 보니 네 가지 한계가 드러났습니다.

첫째, **정적인 정보까지 매번 실시간 조회**했습니다. 셔틀 시간표나 1학생회관 메뉴는 학기 내내 고정인데도 매 질문마다 서버를 찔러서 느리고 불안정했습니다.
둘째, **변형이나 줄임말을 인식 못 했습니다.** 특히 '1학'이라는 줄임말을 이해 못 해서 엉뚱한 답을 냈습니다. 이런 변형을 사람이 무한정 하드코딩해야 했습니다.
셋째, **의미를 고려하지 않는 매칭**이라 일반화가 안 됩니다. 결국 의미 이해를 모델이 아니라 사람이 대신하는 구조였습니다.
넷째, **조회 실패 시 fallback이 오답을 냈습니다.** 실시간 조회가 실패하면 과거 데이터로 답해서 틀린 정보를 줄 위험이 있었습니다.

---

## 슬라이드 9 — ④ 구현: 키워드 → LLM tool-calling 전환 (약 0:55)

이 문제들의 근본 원인은 **의미 판단을 모델이 아니라 키워드 규칙, 즉 사람이 대신하게 만든 것**이었습니다.
빠르고 디버그하기 쉽다는 장점의 대가로, 일반화 불가와 하드코딩, 정적·동적 구분 실패를 떠안은 거죠.

그래서 **Gemma가 직접 도구와 인자를 고르도록** 바꿨습니다.
모델에게 도구 명세를 JSON 스키마로 주면, 어떤 도구를 쓸지와 인자를 한 번에 뽑아냅니다.
예를 들어 '1학'은 제1학생회관으로, '내일'은 날짜로 의미 기반으로 해석합니다.
그리고 **세 가지 상태로 처리**합니다. 도구를 고르거나, 도구가 필요 없다고 하거나, 파싱에 실패하면 기존 키워드 방식으로 폴백합니다. 키워드는 안전망으로만 남겨서 회귀 위험을 줄였습니다.

참고로 12B 모델을 구형 GPU 박스에서 돌리느라 torch 빌드 호환이나 체크포인트 로드, bf16 dtype 같은 구동 이슈도 있었는데, 이건 발표 뒤 질문 주시면 말씀드리겠습니다.

---

## 슬라이드 10 — ⑤ 정성 평가 & 데모 (약 0:50)

정성 평가입니다.
실행은 `chatbot.sh` 하나로 배치 추론과 Streamlit UI가 같이 뜹니다.
챗봇은 텍스트 입력창에 질문을 넣으면 답변과 함께 출처 배지를 보여주고, 멀티턴 대화도 됩니다.

개선 효과를 보면,
키워드에서 LLM 라우팅으로 바꾼 뒤 '1학' 같은 변형 질문에서 도구 호출 정확도가 올랐고,
naive 검색에서 리랭크와 컷오프를 넣은 뒤 무관한 출처가 새어 들어가는 게 확실히 줄었습니다.

예시 질문으로는 "컴퓨터융합학부 졸업 요건", "오늘 1학 뭐 나와", "셔틀버스 시간표" 같은 걸 준비했습니다.
그럼 실제로 한번 시연해 보겠습니다. (→ 데모)

---

## 슬라이드 11 — ⑥ 향후 연구 (약 0:45)

앞으로의 방향입니다.
먼저 라우팅 쪽은, 지금은 LLM 라우팅을 쓰는데 **임베딩 유사도 라우팅과 비용 대비 정확도를 비교**해 보려고 합니다. 그리고 정적·동적 정보를 분리하는 원칙을 명확히 세우겠습니다.
신뢰성 쪽은, **조회 실패 시 정직하게 알리는 fallback 투명화**와, 리랭커 컷오프 임계값을 실제 분포에 맞춰 캘리브레이션하는 걸 하려고 합니다.
성능 쪽은 라우터 출력을 안정화하고 캐싱으로 지연을 줄이는 것, 그리고 여유가 되면 추가 LLM 훈련도 실험해 보겠습니다.

---

## 슬라이드 12 — 마무리 (약 0:15)

이상으로 발표를 마치겠습니다. 들어주셔서 감사합니다. 질문 있으시면 답변드리겠습니다.

---

## 예상 질문 대비 (참고용, 발표엔 미포함)

- **Q. 데이터는 어떻게 만들었나요?**
  A. 충남대 공개 웹을 직접 크롤링해서 정제·청킹한 문서 청크를 벡터 DB에 넣었습니다. 기존 QA 데이터셋은 쓰지 않았습니다.
- **Q. 파인튜닝은 했나요?**
  A. 하지 않았습니다. 베이스 Gemma를 그대로 쓰고, 지식은 RAG로 주입합니다. 그래서 검색 대상도 Q&A가 아니라 문서 청크입니다.
- **Q. 왜 Gemma인가요?**
  A. 오픈 웨이트라 과제 규정을 지키고, 한국어 성능이 좋으며, 4bit 양자화로 T4에서도 구동됩니다.
- **Q. 실시간 정보는 어떻게 최신성을 보장하나요?**
  A. 라우터가 실시간 스킬을 호출해 조회 시점에 갱신합니다. 실패하면 정직하게 확인 실패로 답하는 방향으로 개선 중입니다.
- **Q. 리랭커 컷오프 임계값은 어떻게 정했나요?**
  A. 현재는 보수적으로 잡았고, bge-reranker의 logit 분포를 보고 캘리브레이션하는 게 향후 과제입니다.
